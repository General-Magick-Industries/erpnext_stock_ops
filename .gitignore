# Secrets
**/.env
**/.env.local
**/.env.*.local
*.pem
*.key

# Node / Vite
node_modules/
frontend/dist/
frontend/dist-frappe/
frontend/dev-dist/
**/.vite/
*.log

# Python / Frappe
__pycache__/
*.pyc
*.pyo

# Built PWA bundle — COMMITTED on purpose so `/stock_ops` works on any install
# (vanilla frappe_docker apps.json / bench get-app) WITHOUT a Node/Vite build step.
# Regenerate before committing changes:  cd frontend && npm run build:frappe
# (was previously git-ignored; see README "Buka Aplikasi 404" troubleshooting)
#stock_ops/public/stock_ops/
#stock_ops/www/stock_ops/index.html

# Capacitor native projects (regenerate dgn `npx cap add android`)
frontend/android/
frontend/ios/

# Build artifacts
*.apk
*.aab

# OS / editor
.DS_Store
Thumbs.db
.claude/
